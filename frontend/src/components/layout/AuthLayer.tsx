import Layout from "./Layout";
import AuthModal from "../auth/AuthModal";

const AuthLayer = () => {
  return (
    <div>
      <Layout />
      <AuthModal />
    </div>
  );
};

export default AuthLayer;
