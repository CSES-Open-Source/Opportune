import Layout from "./Layout";
import AuthModal from "../AuthModal";

const AuthLayer = () => {
  return (
    <div>
      <Layout />
      <AuthModal />
    </div>
  );
};

export default AuthLayer;
